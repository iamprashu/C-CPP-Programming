#include<bits/stdc++.h>

int main(){
    std::string name;
    // std::cin>>name;

    std::getline(std::cin,name);
    std::cout<<"Welcome "<<name<<std::endl;

    std::cout<<name.length()<<std::endl;

    return 0;
}